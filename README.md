*[English version](README.en.md)*

# Request Service

[![CI](https://github.com/oneyeeq/ApplicationSystem/actions/workflows/ci.yml/badge.svg)](https://github.com/oneyeeq/ApplicationSystem/actions/workflows/ci.yml)

Платформа для приёма и обработки заявок на услуги: Telegram-бот для клиентов и администраторов, backend на FastAPI, планировщик фоновой архивации и веб-панель на React — четыре независимых процесса, общающихся через реальный слой авторизации, а не через демонстрационную заглушку.

Клиенты оставляют заявки через Telegram-бота. Администраторы просматривают их, принимают в работу, завершают или отклоняют — либо через тот же бот, либо через веб-панель на React. Backend — единый источник истины для обоих клиентов.

## Содержание

- [Архитектура](#архитектура)
- [Ключевые инженерные решения](#ключевые-инженерные-решения)
- [Стек технологий](#стек-технологий)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Конфигурация](#конфигурация)
- [API](#api)
- [Тестирование](#тестирование)
- [Расширение и масштабирование](#расширение-и-масштабирование)
- [Лицензия](#лицензия)

## Архитектура

```
                    ┌─────────────┐
 Пользователь ──────▶  Telegram-  │
   Telegram         │    бот      │──┐
                    └─────────────┘  │
                                      │  HTTP + X-Service-Token
                    ┌─────────────┐  │  (service-to-service авторизация)
 Браузер / React ───▶  FastAPI    │◀─┘
   админ-панель     │    API      │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐      ┌──────────────┐
                    │  PostgreSQL │◀─────│  Планировщик │
                    │             │      │  (архивация) │
                    └─────────────┘      └──────────────┘
```

Четыре независимых процесса, одна база данных, никакого общего состояния в памяти — любой из них можно перезапустить, отмасштабировать или передеплоить, не трогая остальные:

- **`app/`** — backend на FastAPI. Владеет всей бизнес-логикой и всеми записями в базу. Роуты намеренно тонкие (только HTTP-маппинг и перевод исключений в статус-коды); вся остальная логика — в `services/`.
- **`bot/`** — Telegram-бот на aiogram 3. Общается с backend'ом исключительно по HTTP через типизированный клиент (`bot/clients/api_client.py`) — у него нет ни прямого доступа к БД, ни зависимости от SQLAlchemy.
- **`scheduler/`** — отдельный асинхронный цикл, который периодически переносит устаревшие заявки (по умолчанию — старше 30 дней, независимо от статуса) в отдельную таблицу-архив, а не удаляет их безвозвратно. Ходит в базу напрямую — для фоновой задачи без внешних вызывающих нет смысла делать это через API.
- **`migrations/`** — история миграций Alembic, по одной ревизии на изменение схемы, все проверены на обратимость.

## Ключевые инженерные решения

То, на что стоит обратить внимание, если оцениваешь код, а не просто просматриваешь названия папок.

**Две разные модели авторизации, а не одна натянутая на всё.** Человек-администратор, логинящийся в веб-панель, получает JWT access-токен и refresh-токен — потому что это сессия, которая должна пережить перезапуск браузера и быть отзываемой. Бот — доверенный сервис, а не человек с паролем, поэтому он авторизуется общим секретом (`X-Service-Token`, сверяется через `hmac.compare_digest` — устойчиво к timing-атакам), а не притворяется пользователем. Смотри `app/dependencies/auth.py` против `app/dependencies/service_auth.py`.

**Refresh-токены непрозрачны и хранятся хешированными, с ротацией и детектом повторного использования.** В базе хранится не сам токен, а SHA-256-хеш случайного 256-битного значения — утечка базы сама по себе не раскрывает живые сессии. Каждое обновление одновременно инвалидирует старый токен и выдаёт новый; если уже использованный (а значит уже отозванный) токен предъявляется повторно — это трактуется как сигнал кражи, и **все** refresh-токены этого админа отзываются разом. Смотри `app/services/auth_service.py::refresh_access_token`.

**Пароли хешируются через `scrypt`, refresh-токены — через `sha256`, и это осознанное различие.** Пароль короткий и придуман человеком, поэтому нужен медленный, требовательный к памяти хеш для защиты от перебора. Refresh-токен — случайное 256-битное значение без структуры, которую можно перебирать; медленный хеш тут только тратил бы CPU без пользы для безопасности. Использовать один и тот же примитив для обоих случаев — это версия "выглядит аккуратно, а на деле нет".

**Вход защищён рейт-лимитом от перебора пароля.** `POST /auth/login/` ограничен 5 попытками в минуту с одного IP (`slowapi`, `app/rate_limiter.py`) — без этого ничего не мешало бы перебирать пароль администратора неограниченным числом попыток.

**У бота почти нет собственной бизнес-логики.** `bot/services/` только переводит ответы API в кортежи `(успех, сообщение, данные)` и русскоязычные сообщения пользователю. Каждое реальное правило (может ли пользователь создать ещё одну заявку, допустим ли переход статуса) живёт один раз, в backend'е, и бот, и любой будущий клиент автоматически видят одно и то же поведение.

**Веб-панель хранит access-токен только в памяти, refresh — только в httpOnly cookie backend'а.** При старте панель тихо обновляет сессию через `POST /auth/refresh/` (cookie уходит сама, JS её даже прочитать не может); на любом запросе, получившем 401 (access-токен истёк — живёт всего 30 минут), общий перехватчик `useApiFetch` сам обновляет токен и повторяет запрос один раз, и только если это не помогло — разлогинивает. `localStorage` для токена принципиально не используется — это защита от кражи сессии через XSS. Флаг `secure` на cookie с refresh-токеном управляется переменной окружения `COOKIE_SECURE` (`False` для локальной разработки по HTTP, `True` в проде за HTTPS). См. `admin-panel/src/api/useApiFetch.ts`.

**Архивные заявки — отдельная таблица без внешнего ключа, а не флаг `is_archived`.** `ArchivedRequest` не ссылается на `user`: у активной таблицы `request` внешний ключ на пользователя сделан с `ondelete=RESTRICT` специально, чтобы нельзя было случайно потерять историю заявок вместе с удалённым пользователем — а архивная запись, наоборот, должна пережить удаление и пользователя, и оригинальной заявки. Планировщик переносит в архив все заявки старше `REQUEST_STALE_DAYS` дней независимо от статуса — забытая открытая заявка устаревает точно так же, как закрытая.

**Пагинация — отдельный эндпоинт-«близнец», а не смена формы ответа у существующего.** У `GET /requests/` есть ещё один потребитель, кроме веб-панели: бот (`bot/clients/api_client.py`) забирает по нему полный список и сам фильтрует его по статусу и дате, чтобы искать «первую новую заявку» или «заявки за сегодня». Подмена этого эндпоинта пагинированным ответом молча обрезала бы боту видимость данных. Поэтому у каждого списочного ресурса есть параллельный `GET /<ресурс>/paginated?page=&page_size=`, отдающий `{items, total, page, page_size}` через общую generic-схему `PaginatedResponse[T]` (`app/schemas/pagination.py`), а старый безусловный список остаётся как был.

**Тесты разделены по тому, что они реально проверяют.** `tests/*.py` вызывает сервисные и роутовые функции напрямую — быстро, но слепо к тому, что ломается только на уровне настоящего роутинга FastAPI, dependency injection или сериализации Pydantic. `tests/integration/*.py` отправляет настоящие HTTP-запросы через реальное ASGI-приложение (`httpx`/`TestClient`) в изолированную SQLite-базу в памяти, создаваемую заново для каждого теста. Оба слоя существуют, потому что ловят разные классы багов — несколько реальных (неподключённый `Depends`, опечатка в пути роута, рассинхрон схемы ответа, который клиент тихо не мог распарсить) в истории этого проекта были пойманы только вторым видом тестов. На каждый push и pull request оба набора автоматически гоняются в GitHub Actions вместе с `ruff` (`.github/workflows/ci.yml`).

## Стек технологий

| Слой | Выбор |
|---|---|
| API-фреймворк | FastAPI (async), Uvicorn |
| Фреймворк бота | aiogram 3 |
| База данных | PostgreSQL 16 |
| ORM / миграции | SQLAlchemy 2.0 (async), Alembic |
| Авторизация | JWT (`python-jose`), хеширование паролей `scrypt`, HMAC-сервисные токены |
| Валидация | Pydantic v2 |
| Тестирование | pytest, pytest-asyncio, httpx |
| Линтер / форматтер | ruff |
| CI | GitHub Actions — ruff + pytest на каждый push и PR |
| Окружение | Python 3.12, Docker / Docker Compose |
| Веб-панель | React 19 + TypeScript, Vite, React Router, CSS Modules |

## Структура проекта

```
app/                  backend на FastAPI
  routes/              тонкий HTTP-слой — только маппинг запрос/ответ
  services/            бизнес-логика, один модуль на домен (auth, admin, user, request, notification)
  models/              модели SQLAlchemy
  schemas/             Pydantic-схемы запросов/ответов (включая общую pagination.py)
  dependencies/         auth.py (JWT), service_auth.py (сервисный токен бота)
  security.py          хеширование паролей, кодирование/декодирование JWT
  database.py          engine, фабрика сессий

bot/                  Telegram-бот на aiogram
  handlers/            точки входа со стороны Telegram (команды, callback'и)
  services/            переводит ответы API в результаты, понятные боту
  clients/api_client.py  типизированный HTTP-клиент — единственный путь бота к backend'у
  middlewares/         middleware проверки прав админа (один раз на входящее событие)
  keyboards/, presenters/, dtos.py

scheduler/            фоновая задача архивации устаревших заявок
migrations/            ревизии Alembic
requirements/          разбиты по сервисам (base/api/bot/scheduler/dev) — каждый Docker-образ ставит только то, что нужно именно ему
docker/                свой Dockerfile на каждый сервис
tests/                 тесты уровня unit (прямые вызовы функций)
tests/integration/      тесты уровня HTTP против настоящего приложения
enums.py               общий словарь статусов (в корне, чтобы боту не нужно было тянуть слой БД ради одного enum'а)

admin-panel/           веб-панель на React + TypeScript (SPA, Vite)
  src/api/               перехватчик 401 с автообновлением access-токена
  src/auth/              контекст авторизации, защита роутов
  src/components/        общий каркас (сайдбар, таблицы, ErrorBoundary)
  src/pages/             Вход, Заявки, Админы, Пользователи — все три списочные страницы постранично листаются
```

## Быстрый старт

### Docker Compose (рекомендуется)

```bash
cp .env.example .env
# заполни BOT_TOKEN, JWT_SECRET_KEY, SERVICE_TOKEN, POSTGRES_* — см. раздел "Конфигурация"

docker compose up --build
```

Поднимает Postgres, API (миграции применяются автоматически при старте), бота, планировщик и веб-панель — пять контейнеров одной командой. API доступен на `http://localhost:8000` (`/docs` — интерактивная документация OpenAPI), панель — на `http://localhost:5173`. Панель собирается в статику (`npm run build`) и раздаётся через `nginx`, а не через dev-сервер — так же, как остальные сервисы, она едет в контейнере в готовом, а не в режиме разработки виде.

### Локальная разработка

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt   # ставит всё нужное для запуска и тестов локально

cp .env.example .env   # укажи DATABASE_URL на локальную или докеризованную Postgres

alembic upgrade head
uvicorn app.main:app --reload

# в другом терминале
./scripts/restart_bot.sh   # убивает случайно оставшийся процесс бота перед запуском нового —
                            # два поллера с одним BOT_TOKEN вызывают конфликт в Telegram API
                            # и очень запутанное непредсказуемое поведение

# в третьем терминале, если нужен и планировщик
python -m scheduler.main
```

### Веб-панель (React) — локальная разработка панели

Если нужно именно редактировать код панели (не просто пользоваться готовой), Docker для этого неудобен — у dev-сервера Vite есть горячая пересборка, а у собранного образа нет. Для разработки панели отдельно от Docker:

```bash
cd admin-panel
npm install
npm run dev
```

Поднимется на `http://localhost:5173` (том же порту, что и у контейнера — не запускай оба сразу) и обращается к API на `http://localhost:8000`. Убедись, что `CORS_ALLOWED_ORIGINS` в `.env` включает `http://localhost:5173` (по умолчанию так и есть).

## Конфигурация

Все настройки читаются из переменных окружения (`config.py`, через `pydantic-settings`). Полный список с примерами значений — в `.env.example`. Что стоит знать отдельно:

| Переменная | Назначение |
|---|---|
| `BOT_TOKEN` | токен Telegram-бота от [@BotFather](https://t.me/BotFather) |
| `DATABASE_URL` | `postgresql+asyncpg://...` — внутри Docker Compose должен указывать на сервис `db` по имени, а не на `127.0.0.1` |
| `JWT_SECRET_KEY`, `JWT_ALGORITHM`, `JWT_EXPIRE_MINUTES` | подпись access-токена |
| `REFRESH_TOKEN_EXPIRE_DAYS` | срок жизни refresh-токена |
| `SERVICE_TOKEN` | общий секрет, который бот предъявляет API (заголовок `X-Service-Token`) |
| `COOKIE_SECURE` | флаг `Secure` на cookie с refresh-токеном — `True` только за HTTPS, `False` для локальной разработки по HTTP |
| `CORS_ALLOWED_ORIGINS` | JSON-список источников, которым разрешено обращаться к API с credentials (веб-панель) |
| `MAX_ACTIVE_REQUESTS` | сколько открытых заявок может быть у одного пользователя одновременно |
| `REQUEST_STALE_DAYS`, `CLEANUP_INTERVAL_SECONDS` | поведение планировщика архивации |

Генерация секретов:
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

## API

Интерактивная документация генерируется FastAPI автоматически и доступна на `/docs` (Swagger UI) и `/redoc`, когда API запущен.

Коротко:

| Эндпоинт | Авторизация | Назначение |
|---|---|---|
| `POST /auth/login/` | — | Логин админа → access-токен (в теле) + refresh-токен (httpOnly cookie) |
| `POST /auth/refresh/` | refresh-cookie | Ротация access + refresh токена |
| `POST /auth/logout/` | refresh-cookie | Отзыв текущей сессии |
| `GET/POST/PUT/DELETE /admins/*` | JWT | Полный жизненный цикл админ-аккаунтов (самоудаление запрещено) |
| `GET /admins/active` | сервисный токен | Только для бота: активные админы (с привязанным Telegram) для рассылки уведомлений |
| `GET/POST/PUT/DELETE /requests/*` | сервисный токен **или** JWT | Жизненный цикл заявок — доступен и боту, и веб-панели |
| `GET/POST/PUT/DELETE /users/*` | сервисный токен **или** JWT | Записи пользователей — доступны и боту, и веб-панели (удаление блокируется, если есть заявки) |
| `GET /health/` | — | Проверка живости + реального подключения к БД |

У каждого из трёх списочных ресурсов (`requests`, `users`, `admins`) есть дополнительный `GET /<ресурс>/paginated?page=&page_size=` — та же авторизация, что и у остального ресурса, ответ вида `{items, total, page, page_size}` (по умолчанию `page=1`, `page_size=10`, максимум `page_size=100`). Использует веб-панель для таблиц; бот продолжает получать полный список без пагинации там, где ему это нужно.

## Тестирование

```bash
pytest
```

98 тестов, два вида:

- **Unit-уровень** (`tests/`) — сервисы, роуты, презентеры, middleware и сервисный слой бота, проверяются прямым вызовом функций с замоканными зависимостями. Быстро, и точно указывает, какой именно юнит сломался.
- **Интеграционные** (`tests/integration/`) — настоящие HTTP-запросы через реальное FastAPI-приложение (`httpx`/`TestClient`) в изолированную SQLite-базу в памяти, создаваемую заново для каждого теста. Покрывают полный цикл логин → рефреш (ротация + детект повторного использования) → логаут, контроль доступа к защищённым роутам, проверку сервисного токена и форму пагинированных ответов — то, что ломается только на уровне связки компонентов, а не внутри отдельной функции.

Оба набора автоматически прогоняются в GitHub Actions на каждый push и pull request вместе с `ruff check`/`ruff format --check` — конфигурация в `.github/workflows/ci.yml`.

## Расширение и масштабирование

**Добавление нового домена** (ресурса помимо users/requests/admins): модель + миграция, `services/<domain>.py` с бизнес-правилами, `schemas/<domain>_schemas.py`, и тонкий `routes/<domain>_routes.py`, который только переводит исключения в HTTP-статусы. Ровно та же форма, что уже есть у каждого существующего домена — придумывать ничего не нужно.

**Добавление нового сценария в боте**: хендлер в `bot/handlers/`, сервисная функция в `bot/services/`, которая зовёт API-клиент и возвращает кортеж `(успех, сообщение, данные)`, и клавиатура, если нужна. Бизнес-правила по-прежнему живут в backend'е, не здесь — бот должен оставаться тонким слоем представления.

**Несколько реплик API**: API не хранит состояние — ни сессий в памяти, ни refresh-токенов вне Postgres — поэтому можно безопасно запускать несколько экземпляров за балансировщиком нагрузки без изменений в коде. Бот, наоборот, должен оставаться в единственном экземпляре на один `BOT_TOKEN` (Telegram допускает только одного активного получателя обновлений через long polling; см. файловую блокировку в `bot/main.py`).

**Замена инфраструктуры**: Postgres, JWT-секрет и сервисный токен — просто переменные окружения; чтобы указать на управляемую базу данных (RDS, Cloud SQL и т.п.) вместо контейнера из докер-компоуза, изменения в коде не нужны.

**Добавление новой страницы в панель**: страница в `admin-panel/src/pages/`, использующая `useApiFetch()` для запросов (перехватчик сам добавит `Authorization` и обработает истечение токена) и `useAuth()` для доступа к текущей сессии. Маршрут регистрируется в `App.tsx` внутри `ProtectedRoute`, ссылка — в сайдбаре `Layout.tsx`. Ни авторизацию, ни обработку 401 переизобретать не нужно — это уже общая инфраструктура.

## Лицензия

All rights reserved (см. [LICENSE](LICENSE)). Код открыт на GitHub для просмотра и портфолио, но использование, копирование, изменение или коммерческое применение — только с письменного разрешения автора.
